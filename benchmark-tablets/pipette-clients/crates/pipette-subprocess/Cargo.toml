[package]
name = "pipette-subprocess"
version = "0.1.0"
edition = "2021"
publish = false
description = "Process plumbing shared by the pipette client crates: binary lookup, shell-quoted command echoing, SIGPIPE reset, and ^C teardown of spawned children."

[lints]
workspace = true

[dependencies]
anyhow.workspace = true
ctrlc.workspace = true
log.workspace = true
shlex.workspace = true

[target.'cfg(unix)'.dependencies]
libc.workspace = true
