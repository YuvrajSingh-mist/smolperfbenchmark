# Bundled mlx-lm runtime catalog. Each `[[mlx]]` table is a complete
# bundled runtime entry, keyed by `version` (the mlx-lm version).
#
# Each entry carries:
#   - `version`      — the mlx-lm version (the catalog key / `mlx-macos-pipette://version=`)
#   - `flavor`       — the hardware build target (`macos-arm64`)
#   - `requirements` — the exact locked text materialized into
#                      `requirements.txt` during install
#
# `requirements` is embedded here so this TOML file is the single
# source of truth for the bundled catalog.

[[mlx]]
version = "0.31.3"
flavor = "macos-arm64"
requirements = '''
annotated-doc==0.0.4
anyio==4.14.1
certifi==2026.6.17
click==8.4.2
filelock==3.29.7
fsspec==2026.6.0
h11==0.16.0
hf-xet==1.5.1
httpcore==1.0.9
httpx==0.28.1
huggingface-hub==1.22.0
idna==3.18
jinja2==3.1.6
markdown-it-py==4.2.0
markupsafe==3.0.3
mdurl==0.1.2
mlx==0.32.0
mlx-lm==0.31.3
mlx-metal==0.32.0
numpy==2.5.1
packaging==26.2
protobuf==7.35.1
pygments==2.20.0
pyyaml==6.0.3
regex==2026.6.28
rich==15.0.0
safetensors==0.8.0
sentencepiece==0.2.1
shellingham==1.5.4
tokenizers==0.22.2
tqdm==4.68.4
transformers==5.12.1
typer==0.26.8
typing-extensions==4.16.0
'''
