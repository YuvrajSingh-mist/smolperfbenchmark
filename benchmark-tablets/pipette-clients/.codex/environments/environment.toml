# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "iOS app"

[setup]
script = '''
#!/usr/bin/env bash
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

git submodule update --init --recursive vendor/llama.cpp
rustup target add aarch64-apple-ios aarch64-apple-ios-sim

./ios/build-rust.sh sim
'''

[cleanup]
script = '''
#!/usr/bin/env bash
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

rm -rf ios/DerivedData ios/build ios/Pipette/build
find ios/Pipette/Pipette/Generated -mindepth 1 ! -name .gitkeep -delete
'''
